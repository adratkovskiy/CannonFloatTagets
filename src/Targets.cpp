#include "stdafx.h"
#include "Targets.h"

Targets::Targets(const float size, const float speed, const FPoint& pos, const FPoint& direction, const int points):
	_size(size)
	, _speed(speed)
	, _pos(pos)
	, _direction(direction)
	, _points(points)
{
}
